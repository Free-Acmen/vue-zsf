
const state = {
    listData: {}
}

const getters = {}

const actions = {}

const mutations = {
    setListData(state, data){
        state.listData = data
    }
}

export default{
    state,
    getters,
    actions,
    mutations
}